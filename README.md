# Portal

## Usags with docker-compose

When cloning from git for the first time, run this in the root folder:

```bash
composer install
```

Then to run it:

```bash
docker-compose up -d --build
docker-compose down
```
